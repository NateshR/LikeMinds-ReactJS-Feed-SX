// src/Header/Header.tsx

import React from "react";

const Header: React.FC = () => {
  return (
    <div>
      <h1>Suraasa</h1>
      {/* Add header content */}
    </div>
  );
};

export default Header;
